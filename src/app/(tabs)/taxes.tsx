import { PlaceholderScreen } from '@/components/placeholder-screen';

/** Quarterly tracker (PRD §8.6). */
export default function TaxesScreen() {
  return (
    <PlaceholderScreen
      title="Taxes"
      subtitle="Your quarterly payments and deadlines will appear here."
    />
  );
}
